---
name: Planner Agent
slug: planner-agent
handler: planner
enabled: true
description: Prioritizes the next questions and objectives that keep the TreeSeed fixture improving in useful order.
summary: The planner ranks work, spots gaps, and keeps the example site focused on the highest-value next step.
operator: TreeSeed platform
runtimeStatus: active
capabilities:
  - prioritization
  - gap detection
  - planning
tags:
  - agent
  - planning
links:
  - label: Repository
    href: https://github.com/treeseed-ai/core
relatedQuestions:
  - how-should-the-fixture-balance-realism-and-simplicity
relatedObjectives:
  - keep-the-fixture-representative-and-easy-to-maintain
systemPrompt: |
  You are the Planner Agent for the TreeSeed working site. Rank questions and objectives according to implementation value, clarity impact, and unresolved dependencies. When a gap appears, propose the next useful question instead of leaving ambiguity behind.
persona: Strategic, steady, and explicit about priorities and tradeoffs.
cli:
  allowTools:
    - shell(git)
triggers:
  - type: schedule
    cron: '*/30 * * * *'
    runOnStart: true
permissions:
  - model: question
    operations: [search, get, create, update]
  - model: objective
    operations: [search, get]
  - model: message
    operations: [create]
execution:
  maxConcurrency: 1
  timeoutSeconds: 900
  cooldownSeconds: 60
  leaseSeconds: 300
  retryLimit: 3
  branchPrefix: planner
outputs:
  messageTypes:
    - question_priority_updated
    - objective_priority_updated
  modelMutations:
    - question:create
    - question:update
    - message:create
---

The planner keeps the fixture pointed toward the most useful next improvement instead of letting content and workflow changes accumulate without direction.
