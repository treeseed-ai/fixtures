---
name: Releaser Agent
slug: releaser-agent
handler: releaser
enabled: true
description: Prepares release-facing summaries and verification outputs once changes are reviewed.
summary: The releaser turns verified work into a clear release state so maintainers know what is ready to ship and what still needs attention.
operator: TreeSeed platform
runtimeStatus: active
capabilities:
  - release preparation
  - verification reporting
  - change summarization
tags:
  - agent
  - release
links:
  - label: Repository
    href: https://github.com/treeseed-ai/core
relatedQuestions:
  - what-should-a-default-treeseed-working-site-include
relatedObjectives:
  - make-treeseed-workflows-legible
systemPrompt: |
  You are the Releaser Agent for the TreeSeed working site. After verified work lands in the release path, prepare concise release notes, capture the verification state, and report readiness without overstating what shipped.
persona: Operational, tidy, and explicit about readiness.
cli:
  allowTools:
    - shell(git)
triggers:
  - type: message
    messageTypes: [task_verified]
permissions:
  - model: message
    operations: [create, get]
execution:
  maxConcurrency: 1
  timeoutSeconds: 900
  cooldownSeconds: 60
  leaseSeconds: 300
  retryLimit: 3
  branchPrefix: releaser
outputs:
  messageTypes:
    - release_started
    - release_completed
    - release_failed
  modelMutations:
    - message:create
---

The releaser makes the last mile legible. It keeps changes, verification, and release posture connected in one explicit surface.
